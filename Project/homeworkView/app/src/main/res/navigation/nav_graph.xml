<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/scrollingFragmentHWView">

    <fragment android:id="@+id/placeholder" />
    <fragment
        android:id="@+id/scrollingFragmentHWView"
        android:name="com.firstapp.homeworkview.ScrollingFragmentHWView"
        android:label="fragment_scrolling_h_w_view"
        tools:layout="@layout/fragment_scrolling_h_w_view" >
        <action
            android:id="@+id/action_scrollingFragmentHWView_to_mainActivity"
            app:destination="@id/mainActivity" />
    </fragment>
    <activity
        android:id="@+id/mainActivity"
        android:name="com.firstapp.homeworkview.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
</navigation>